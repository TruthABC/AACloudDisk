<resources>

    <!-- Base application theme. -->
    <style name="Theme.AACloudDisk" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorEdgeEffect">@color/gray_deep_half</item>
    </style>

</resources>
